WITH MHI AS (
    SELECT * FROM {{ref('stg_mhi')}}
),
CPI AS (
    SELECT * FROM {{ref('stg_yearly_cpi')}}
)

SELECT * FROM MHI
JOIN CPI ON CPI.CPI_BY_YEAR = MHI.MHI_BY_YEAR
WHERE CPI.CPI_BY_YEAR >= '1980' AND CPI.CPI_BY_YEAR <= '2020'
ORDER BY CPI.CPI_BY_YEAR